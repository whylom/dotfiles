function seconds(n) {
  return 1000 * n;
}

function minutes(n) {
  return seconds(60) * n;
}

// var selectors = [
//   '#sharing',
//   '#sidebar',
//   '#sidebar1',
//   '.sidebar'
// ].join(',');

// $(selectors).css('opacity', 0.1);

// $(selectors).hover(function() {
//   $(this).css('opacity', 1);
// }, function() {
//   $(this).css('opacity', 0.1);
// });
