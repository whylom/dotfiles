// Hide header (page looks slightly better without it)
$('.wds-global-navigation-wrapper').remove();

// Hide grotesque geek culture clickbait
$('#WikiaRailWrapper').remove(); // sidebar
$('footer').remove();
